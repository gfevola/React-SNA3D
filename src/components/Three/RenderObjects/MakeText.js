//import TextSprite from '@seregpie/three.text-sprite';

function MakeText(textStr,color,font_size,position){

	/* 	let instance = new TextSprite({
		  alignment: 'left',
		  color: color,
		  fontFamily: '"Times New Roman", Times, serif',
		  fontSize: font_size,
		  fontStyle: 'regular',
		  text: [
			textStr,
		  ].join('\n'),
		});
		instance.position.set(position[0],position[1],position[2])
		 */
		
	let instance = []	
return(instance)
}
export default MakeText;